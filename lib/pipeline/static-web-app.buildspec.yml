version: "0.2"
phases:
  install:
    commands:
      - npm i -g yarn
      - yarn install
  # pre_build:
  #   commands:
  #     - yarn lint
  #     - yarn test
  build:
    commands:
      - yarn build
artifacts:
  base-directory: build
  files:
    - "**/*"
